{
  "name": "@kopai/app",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "lint": "eslint src",
    "type-check": "tsc --noEmit",
    "start": "node ./dist/index.js",
    "start:dev": "node --env-file=.env --watch-path=./dist ./dist/index.mjs"
  },
  "dependencies": {
    "@fastify/swagger": "^9.6.1",
    "@fastify/swagger-ui": "^5.2.4",
    "@kopai/core": "workspace:*",
    "@kopai/api": "workspace:*",
    "@kopai/collector": "workspace:*",
    "fastify": "^5.7.0",
    "fastify-type-provider-zod": "^6.1.0"
  },
  "devDependencies": {
    "@kopai/tsconfig": "workspace:*",
    "tsdown": "^0.19.0"
  }
}
