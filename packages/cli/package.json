{
  "name": "@kopai/cli",
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "kopai": "./dist/index.mjs"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "lint": "eslint src",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@kopai/core": "workspace:*",
    "@kopai/sdk": "workspace:*",
    "commander": "^14.0.2"
  },
  "devDependencies": {
    "@kopai/tsconfig": "workspace:*",
    "tsdown": "^0.20.1"
  }
}
